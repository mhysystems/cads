// scan sent to server
// Use capitals for names to reduce dotnet compiler warnings
namespace CadsFlatbuffers;

table profile{
  y : double;
	x_off : double;
	z_samples : [float];
}

table profile_array {
  idx   : ulong;
  count : ulong;
  profiles : [profile];
}

table Conveyor {
  site : string;
  name : string;
  timezone : string;
  pulley_circumference : double;
  typical_speed : double;
}

table Belt {
  serial : string;
  pulley_cover : double;
  cord_diameter : double;
  top_cover : double;
  length : double;
  width : double;
  width_n : uint;
}

table register_scan {
  belt_serial : string;
}

table Install {
  belt : Belt;
  conveyor : Conveyor;
}


union scan_tables {Install, profile_array, register_scan}

table scan {
  site : string;
  conveyor_name : string;
  chrono : string;
  contents : scan_tables;
}

root_type profile;